int least_byte(int);
int complement_all_but_least(int);
int least_set_to_one(int x);

/* bitset : sets all bits of x which corresponds to set bits of m */
int bis(int x, int m);

/* bitclear : clears all bits of x which corresponds to set bits of m */
int bic(int x, int m);

int bool_or(int x, int y);
int bool_xor(int x, int y);
